{
  "name": "@johnnren/mcp-searxng-public-extended",
  "version": "0.1.6",
  "description": "MCP server for SearXNG web search with HTML parsing, parallel requests and throttling",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-searxng-public-extended": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "node dist/index.js",
    "patch": "npm version patch && git push --follow-tags",
    "minor": "npm version minor && git push --follow-tags",
    "major": "npm version major && git push --follow-tags"
  },
  "keywords": [
    "mcp",
    "searxng",
    "search",
    "web-search"
  ],
  "author": "johnnren",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/john-walks-slow/mcp-searxng-public.git"
  },
  "bugs": {
    "url": "https://github.com/john-walks-slow/mcp-searxng-public/issues"
  },
  "homepage": "https://github.com/john-walks-slow/mcp-searxng-public#readme",
  "dependencies": {
    "fastmcp": "^3.34.0",
    "undici": "^7.24.2",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
